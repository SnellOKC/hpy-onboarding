extends layout

block content

  h1 Delete Candidate: #{candidate.name}
  
    p Do you really want to delete this Candidate?
    
    form(method='POST' action='')
      div.form-group
        input#id.form-control(type='hidden',name='id', required='true', value=candidate._id )

      button.btn.btn-primary(type='submit') Delete